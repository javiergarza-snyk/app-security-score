Drop a corporate root CA `.crt` file here if your network does TLS
interception (e.g. Zscaler) and the Docker build can't reach npm/PyPI/Snyk.
Files in this directory (except this README/.gitignore) are gitignored —
they never get committed or published.
